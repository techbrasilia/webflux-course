
spring:
  data:
    mongodb:
      uri: mongodb+srv://${DB_USER:admin}:${DB_PASSWORD}@webfluxcoursecluster.bdepgu1.mongodb.net/webflux-course?retryWrites=true&w=majority&appName=WebfluxCourseCluster
      auto-index-creation: true
  application:
    name: webflux-course